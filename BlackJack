package blackjack;

/**
 *
 * @authors James Tomasko and Kelsey Studeman
 */
import java.util.*;
import javax.swing.JOptionPane;

public class BlackJack {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        boolean done = false;
        int choice, buttons;
        Deck deck;

        String[] choices = {
            "New Game",
            "Load Game",
            "Exit"};

        while (!done) {

            choice = JOptionPane.showOptionDialog(null,
                    "",
                    "Game Menu",
                    JOptionPane.YES_NO_CANCEL_OPTION,
                    JOptionPane.QUESTION_MESSAGE,
                    null,
                    choices,
                    choices[0]);

            switch (choice) {

                case 0:
                    String[] options = {
                        "Hit",
                        "Stand",
                        "Next Round",
                        "Save Game",
                        "Exit"};

                    while (!done) {

                        buttons = JOptionPane.showOptionDialog(null,
                                "",
                                "Game Menu",
                                JOptionPane.YES_NO_CANCEL_OPTION,
                                JOptionPane.QUESTION_MESSAGE,
                                null,
                                options,
                                options[0]);

                        switch (buttons) {

                            case 0:

                                break;

                            case 1:

                                break;

                            case 2:

                                break;
                                
                            case 3:
                                
                                break;

                            default:
                                JOptionPane.showMessageDialog(null, "Exiting Game...");
                                done = true;
                                break;

                        }
                    }
                    break;

                case 1:

                    break;

                default:
                    JOptionPane.showMessageDialog(null, "Exiting Game...");
                    done = true;
                    break;

            }
        }

    }
}
